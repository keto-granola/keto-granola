name: Build frontend
description: Installs frontend deps and builds Vite assets so backend Go embeds resolve

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup-node

    - name: Build frontend
      shell: bash
      working-directory: frontend
      run: npm run build